import { ID as DamagableName } from "./Damagable";
import { ID as MoveableName } from "./Moveable";
import { ID as VisionName } from "./Vision";

export const MixinNames = {
    moveable: MoveableName,
    damagable: DamagableName,
    vision: VisionName
};